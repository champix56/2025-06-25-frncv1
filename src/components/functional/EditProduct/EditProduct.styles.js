import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  EditProduct: {},
  imageContainer: { justifyContent: "center",alignItems:'center', marginVertical: 100 },
  image: {
    width: 256,
    height: 256,
  },
});

export default styles;
